[build]
  command = "npm run build"
  # For Remix + Vite the client assets are output to `build/client`.
  # Netlify should publish that folder so static assets and the SPA entry are available.
  publish = "build/client"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
